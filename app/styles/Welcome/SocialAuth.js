import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    columnGap: 24,
    position: "absolute",
    bottom: 0,
    marginBottom: 32,
  },
});

export default styles;
