import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  map: {
    width: "100%",
    height: "100%",
  },

  carMarker: {
    width: 36,
    height: 36,
  },
});

export default styles;
